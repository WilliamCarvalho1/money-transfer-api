Money Transfer API

📌 Overview

The Money Transfer API is a RESTful service developed in Java 17 that allows scheduling, updating, deleting, and querying bank transfer transactions.
The system automatically calculates transfer fees based on the transaction amount and the scheduled date, following predefined business rules.